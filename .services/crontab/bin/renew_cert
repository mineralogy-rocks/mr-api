#!/usr/bin/env bash
set -e

cd /home/ubuntu/backend
/usr/local/bin/docker compose -f docker-compose.certbot.yml run --rm --no-deps certbot-renew
# /usr/local/bin/docker compose -f docker-compose.prod.yml restart nginx
